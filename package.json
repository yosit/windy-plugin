{
  "name": "@yosit/windy",
  "version": "1.0.7",
  "description": "Windy API client and shared runtime for Runline and Dripline plugins",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "windy-install": "dist/install.mjs"
  },
  "files": [
    "dist/",
    "skills/",
    "plugins/",
    "README.md"
  ],
  "workspaces": [
    "./",
    "plugins/*"
  ],
  "scripts": {
    "build": "tsc",
    "bundle": "bun run build && node scripts/build-bundles.mjs",
    "smoke": "node scripts/smoke.mjs",
    "prepare": "bun run build && bun run link-workspace",
    "link-workspace": "node scripts/link-workspace.mjs",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "claude",
    "claude-code",
    "skill",
    "windy"
  ],
  "author": "yosit",
  "license": "MIT",
  "packageManager": "bun@1.4.2",
  "engines": {
    "node": ">=16"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "dependencies": {
    "https-proxy-agent": "^7.0.5"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.0",
    "vite": "^7.3.6",
    "vitest": "^5.0.1"
  }
}
